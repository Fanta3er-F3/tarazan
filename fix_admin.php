<?php
require_once 'config.php';
try {
    // Додаємо тип транзакції для виплат, якщо раптом його немає (хоча SQLite це проковтне)
    // У SQLite типи ENUM не жорсткі, тому ми просто будемо використовувати нові назви типів у коді.
    echo "<h3>✅ База готова до розширених функцій адміна!</h3>";
    echo "<a href='settings.php'>Перейти в Адмін-панель</a>";
} catch (Exception $e) { echo $e->getMessage(); }
?>